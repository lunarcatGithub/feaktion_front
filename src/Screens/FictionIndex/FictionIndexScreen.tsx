import React from "react";
import FictionIndex from "~/Components/FictionIndex/FictionIndex";

export default function FictionIndexScreen({ navigation, route }: any) {
  return <FictionIndex navigation={navigation} route={route} />;
}
