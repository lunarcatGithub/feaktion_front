import React from "react";
import AllNotification from "~/Components/Notification/All_Notification";

export default function AllNotificationScreen({
  navigation,
}: any): JSX.Element {
  return <AllNotification navigation={navigation} />;
}
