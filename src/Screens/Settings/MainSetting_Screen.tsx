import React from "react";

// components
import MainSetting from "~/Components/Settings/MainSetting";

export default function MainSettingScreen({ navigation, route }: any) {
  return <MainSetting navigation={navigation} route={route} />;
}
